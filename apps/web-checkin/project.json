{
  "name": "web-checkin",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/web-checkin",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/web-checkin",
        "postcssConfig": "apps/web-checkin/postcss.config.js"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/web-checkin"
        },
        "staging": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "web-checkin:build",
        "port": 4206,
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "web-checkin:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "web-checkin:build:production",
          "dev": false
        },
        "staging": {
          "buildTarget": "web-checkin:build:staging",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "web-checkin:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/web-checkin/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/web-checkin/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
